{
  "name": "opencode-webhook-notify",
  "version": "0.0.0",
  "description": "OpenCode plugin that sends customizable webhook notifications on session completion and permission requests.",
  "main": "src/index.ts",
  "module": "src/index.ts",
  "type": "module",
  "files": [
    "src",
    "README.md"
  ],
  "keywords": [
    "opencode",
    "plugin",
    "webhook",
    "notification",
    "ai"
  ],
  "license": "MIT",
  "peerDependencies": {
    "@opencode-ai/plugin": "*"
  },
  "devDependencies": {
    "@opencode-ai/plugin": "latest",
    "@types/bun": "latest",
    "typescript": "latest"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "update-version": "bun scripts/update-version.ts",
    "prepublishOnly": "bun run update-version"
  }
}
